const initialState = [
    {test: "It works", test2:"Of course"}
]

export const reducer = ( state = initialState, action) => {
    switch (action.type) {
        default:
            return state
    }
}