{
    "version": 2,
    "name": "project-name",
    "builds": [{ "src": "./package.json", "use": "@now/static-build", "config": {"distDir": "public"} }],
    "routes": [
        { "src": "/(.*)", "dest": "./$1" }
    ]
}